/**
 * @deprecated Import from ./config/env.js instead.
 * Re-exports kept for any external deep imports during transition.
 */
export {
  getApiBaseUrl,
  getVirtualKey,
  getPat,
  getWorkspaceId,
  getRateLimitRpm,
  requireBaseUrl,
} from "./config/env.js";
